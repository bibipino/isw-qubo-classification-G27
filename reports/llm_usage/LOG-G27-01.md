# LLM Interaction Log

This file records all relevant interactions with Large Language Models (LLMs) used during the project.

Each interaction must be recorded as a separate entry. You must use the following unique interaction ID: `G03-06-12-001`, `G03-06-12-002`, etc., where GXX is the group identifier, MM-DD means month and day, followed by a progressive number starting from 001 for the first recording of the day.

Do **not** overwrite previous entries. Do **not** use the same downloaded file name twice. All downloaded files must be stored in the project repository and referenced exactly with their relative path.

The same day must not be covered by more than one interaction log. If you feel that the interaction log is becoming too long, at the end of the day close the log and start another one the following day. The logs are identified by their log_id, with the group code and a progressive number NN: "GXX-NN". Each log must be contained in a file named: "LOG-GXX-NN.md". 

---

## Metadata

```yaml
group_id: "G27"
repository_url: "https://github.com/bibipino/isw-qubo-classification-G27"
students:
  - matricola: "60/61/66501"
    name: "Nonnis Gabriele"
  - matricola: "60/61/66529"
    name: "Maderazo Jerome"
last_update: "2026-07-24"
log_id: "G27-01"
```

---

# Interaction Entries

---

## Interaction G27-07-22-001

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "QUBO Feature Selection Project Analysis"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "requirements_understanding"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Analyse these specifications for the project: Ingegneria del Software
Prova pratica AA 2025-26 – Progetto Python
Classificazione binaria con feature reduction QUBO
Durata: 2 settimane dalla data della consegna1
Descrizione sintetica del progetto
Il progetto riguarda lo sviluppo di un’applicazione per classificare dei dati contenuti in un dataset,
chiamati “campioni” o “record”. Si tratta di una classificazione binaria, cioé dell’assegnazione di un
campione a una classe a due valori, come ad es. “sano”/”malato”, “rischio basso/rischio alto”,
“amico/nemico”.
Il dato da classificare è un record, composto da una lista di campi, che nel nostro caso sono
numerici.
Il dataset iniziale ha i campioni già classificati, cioè per ogni campione si conosce la sua classe.
Esso va diviso in due sottoinsiemi. Uno, chiamato “training set”, serve all’addestramento del
classificatore; l’altro, chiamato “test set” serve a valutare la qualità del classificatore addestrato sul
training set.
Si può considerare il dataset come una matrice, le cui righe sono i campioni, e le cui colonne sono
i campi, chiamati anche “features” (caratteristiche); i valori noti di ogni campioni sono contenuti in
un vettore binario (di 0 e 1), il cui elemento i-esimo rappresenta la classe del campione i-esimo.
Più in dettaglio, la classificazione proposta riguarda la determinazione se una persona che richiede
un prestito sia affidabile o a rischio. Il programma sviluppato deve però semplicemente gestire un
dataset numerico dato, con colonna target di zeri e uni.
Ogni richiesta di prestito è caratterizzata da un dato numero di campi (chiamati “features”) che
descrivono il richiedente, con dati anagrafici, lavorativi, finanziari, e altri. L’output è un valore
binario: “0” per richiedente affidabile, “1” per richiedente a rischio.
Se i campi sono troppi (ad es., il dataset usato per le prove ha 145 features), prima di predisporre il
classificatore occorre ridurre il numero di features, con una procedura di ottimizzazione.
In sintesi, le operazioni da svolgere, che saranno descritte in dettaglio in seguito, sono:
1. Lettura del dataset per la riduzione delle features e l’apprendimento.
2. Conversione di tutti i campi dei record a valori numerici normalizzati. Eliminazione
preliminare delle features (colonne) che contengono una percentuale di valori nulli o non
definiti superiore a una data soglia (di solito intorno all’90-95%).
3. Divisione del dataset in training set e test set. È effettuata considerando i primi M campioni
come training set, e i campioni restanti come test set. M è fornito in input al programma.
4. Determinazione di quali features eliminare e quali usare per la classificazione, usando i dati
del training set e con una procedura di ottimizzazione QUBO.
5. 6. 7. Eliminazione delle features non selezionate sia dal training set che dal test set.
Addestramento di un classificatore sui record del training dataset così ridotto.
Classificazione sui dati del test set, confronto coi valori reali e stampa dei risultati.
L’applicazione dovrà essere dotata di un’interfaccia utente per controllare e visualizzare lo stato
delle varie fasi dell’elaborazione, e di test automatici per la verifica del sistema
Gli algoritmi ed approcci non sono determinati, tranne la procedura per calcolare la funzione costo
per la riduzione delle features.
1 In caso di problemi, si può richiedere un’estensione al docente.
1Il programma sviluppato deve poter elaborare dataset di dimensioni qualsiasi (sia righe che
colonne). L’unico vincolo è che i dati siano già in forma numerica. Il vettore target è contenuto in
una colonna del dataset il cui nome è fornito in input.
Il progetto deve essere sviluppato da un gruppo formato da una coppia da due studenti, utilizzando
un LLM, e consegnato tramite repository GitHub. In casi particolari, potrà essere autorizzato un
“gruppo” formato da un solo studente; in tale caso, il progetto rimane comunque invariato.
2. Vincoli generali
Il progetto deve essere sviluppato in Python 3.11 o superiore.
Vanno usate librerie open source, scelte dal gruppo anche con l’ausilio del LLM.
È obbligatorio usare almeno lo strumento LLM/AI prescritto per il gruppo. L’interazione deve
avvenire in lingua inglese. L’uso deve essere documentato in grande dettaglio.
3. Repository GitHub
Ogni gruppo deve creare un repository GitHub con nome:
isw-qubo-classification-GXX
dove GXX è il codice del gruppo, ad esempio:
python-qubo-classification-G01
python-qubo-classification-G02
Il repository deve essere accessibile agli utenti Github: micheledmi, Druimo e Piras78.
4. Struttura obbligatoria del repository
Il repository deve avere esattamente questa struttura minima:
isw-qubo-classification-GXX/
│
├── README.md
├── requirements.txt
├── group_info.yaml
│
├── data/
│ └── input_dataset.csv
│ └── sample_test_dataset.csv
│
├── src/
│ └── qubo_project/
│ ├── __init__.py
│ ├── preprocessing.py
│ ├── feature_selection.py
│ ├── model.py
│ └── gui.py # opzionaleo simile per la GUI
│
├── tests/ # questa cartella contiene i test automatici
2│
│
├── outputs/
└── reports/
├── project_report.yaml
└── llm_usage/
├── LOG-GXX-01.md
├── LOG-GXX-02.md
├── ...
└── files/
├── LLM_GXX-MM-DD-001_preprocessing_function_1.py
├── LLM_GXX-MM-DD-001_qubo_creation_1.py
├── LLM_GXX-MM-DD-001_final_report_draft.yml
└── . . .
La cartella outputs/ può essere vuota alla consegna. Sarà usata dal programma di valutazione
per salvare risultati, modelli e predizioni.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Here is a structured analysis of your Software Engineering project specifications.
The project requires building a complete machine learning pipeline with a specific focus on QUBO (Quadratic Unconstrained Binary Optimization) for feature selection, combined with strict software engineering practices (version control, automated testing, and detailed LLM interaction logging).
1. The Core Workflow (Pipeline Breakdown)
Your application needs to execute a sequential pipeline. Here is how it translates into software modules:
Data Ingestion & Preprocessing:
You need to read a numeric CSV dataset and a specified target column.
Crucial constraint: Drop columns with >90-95% null/undefined values.
Data Normalization: All remaining features must be normalized (e.g., Min-Max scaling or Z-score standardizing) so the QUBO algorithm and the classifier handle them correctly.
Non-Standard Train/Test Split:
Watch out here: The specification explicitly states you must use the first M samples for training and the rest for testing, based on a user input M. Do not use standard randomized splits (like scikit-learn's train_test_split) unless you configure them to not shuffle the data.
QUBO Feature Selection:
This is the technical heart of the project. You must formulate feature selection as a QUBO problem using the training data to calculate a cost function. You will likely need to use libraries like dimod (from D-Wave) to construct the QUBO and a solver (like Simulated Annealing) to find the optimal binary vector where 1 means "keep feature" and 0 means "drop feature".
Classification:
Apply the feature mask from the QUBO step to both the training and test sets.
Train a standard binary classifier (e.g., Logistic Regression, Random Forest, or SVM). The specific algorithm is left up to you.
Evaluate on the test set and output metrics (Accuracy, Precision, Recall).
2. Software Architecture & Deliverables
The project enforces strict structural and methodological constraints:
Technology Stack: Python 3.11+. You are free to choose open-source libraries (e.g., pandas, numpy, scikit-learn, PyQt or Tkinter for GUI).
User Interface (GUI): A visual interface is required to track the pipeline's progress. It should likely have inputs for the dataset path, target column name, the M value for the split, and display areas for the selected features and final accuracy metrics.
Testing: Automated tests are mandatory. You will need to populate the tests/ folder, likely using pytest or unittest, to verify individual functions (e.g., testing the preprocessing logic on a dummy dataset).
3. Strict Compliance Rules
Failing these will likely result in immediate penalties:
LLM Usage & Logging: Your interactions with the LLM must be in English and heavily documented. The reports/llm_usage/ folder requires a very specific naming convention for logs (LOG-GXX-01.md) and generated code files (LLM_GXX-MM-DD-001_...py).
Repository Structure: The folder structure provided in the specs is mandatory. Do not deviate from the src/qubo_project/ setup.
GitHub Access: Ensure users micheledmi, Druimo, and Piras78 are added as collaborators to isw-qubo-classification-GXX.
Project Risk Area: The QUBO formulation is usually the most mathematically complex part. You will need to define a cost function that balances maximizing the relevance of the features to the target variable while minimizing the redundancy between the selected features.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: false
    short_description: null
    referenced_in_response: false
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"    # "yes"/"no"  The subsequent part only if "yes"
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 4
  correctness_1_to_5: 4
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 4
  would_reuse_this_output: "yes"
  notes: null
```

---

## Interaction  G27-07-22-002

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "QUBO Feature Selection Project Analysis"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: "trial_dataset_ISW.csv"
      repository_path: "data/trial_dataset_ISW.csv"
      description: "Input dataset for the preprocessing phase"
    - file_name: "training.csv"
      repository_path: "data/training.csv"
      description: "Target output file for training data"
    - file_name: "test.csv"
      repository_path: "data/test.csv"
      description: "Target output file for testing data"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Help me generate a ChatGPT prompt to meet these initial requirements:
6. Dataset
Il dataset viene fornito al sistema in formato CSV.
Il dataset contiene:
• N colonne di input con campi numerici;
• una ulteriore colonna target per la classificazione binaria;
3• alcune colonne possono essere vuote o quasi vuote, o contenere tutti zeri, o quasi.
Il nome della colonna target sarà passato al programma tramite parametro di input.
Il codice non deve assumere che il target abbia sempre lo stesso nome.
Il codice deve supportare target binario codificato come: 0 / 1
Sarà fornito un dataset, di nome: input_dataset.csv, per effettuare le prove, con N=145
features più il target, e 20.000 campioni. Questo non è il dataset che sarà usato per la verifica del
programma sviluppato.
Il dataset di verifica avrà più di 1.500.000 campioni, quindi i programmi sviluppati devono poter
gestire dataset di tali dimensioni.
7. Fase 1: preprocessing del dataset
Il modulo di preprocessing deve:
1. leggere il CSV con tutto il dataset di input;
2. separare la colonna target;
3. eliminare colonne vuote o quasi vuote, ovvero con tutti zeri o quasi; la percentuale di
colonne vuote o con zeri sul totale sotto la quale la colonna è eliminata è un parametro dato
in input prima dell’elaborazione;
4. normalizzare le feature, producendo un dataset finale numerico e normalizzato;
7.1 Colonne vuote o quasi vuote
Una colonna può essere eliminata se contiene troppi valori mancanti o pari a zero.
La soglia di rimozione è data in input al programma. Ad esempio, potranno essere rimosse colonne
con più del 95% di valori mancanti o nulli.
7.2 Normalizzazione
Le feature devono essere normalizzate usando la standardizzazione z-score, cioè i valori sono
trasformati con una trasformazione lineare in modo da avere media nulla e deviazione standard
unitaria.
La normalizzazione deve essere applicata sull’intero dataset, escluso ovviamente la colonna col
target.
7.3 Divisione del dataset
Il dataset è suddiviso considerando i primi M campioni come training set, e i campioni restanti
come test set. Il parametro M va calcolato in base alla percentuale del test set sul totale dei
campioni, fornita in input. La divisione del dataset va fatta con un “taglio netto” al campione M.
Entrambi i dataset (training e test) sono salvati su file .csv.
Remember the repository structure and specify where the data is located.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Here is a ChatGPT prompt carefully constructed to meet all the specified requirements for the preprocessing phase, adhering to your exact file names and repository structure.

---

**Copy and paste the following text into ChatGPT:**

> Act as an expert Python software engineer. I am working on a machine learning project with strict structural and algorithmic requirements, and I need you to write the complete code for the data preprocessing module.
> The code should be saved as `preprocessing.py` and placed within my existing repository structure: `src/qubo_project/preprocessing.py`.
> **Project Context & Data:**
> * The input dataset is a CSV file containing numeric features and one binary target column (coded as 0 and 1).
> * The code must be highly efficient as it will eventually be tested on a dataset with over 1,500,000 samples.
> * For development, we are using a trial dataset named `trial_dataset_ISW.csv` located in the `data/` directory.
> * The resulting output files must be saved back to the `data/` directory as `training.csv` and `test.csv`.
> 
> 
> **Preprocessing Requirements:**
> Please write a Python script (using libraries like `pandas` and `numpy`) that performs the following sequential steps:
> 1. **Dynamic Inputs:** The main execution or function must accept the dataset file path, the exact name of the target column (do not hardcode this), a threshold percentage for dropping columns, and a test set percentage to determine the split.
> 2. **Target Separation:** Read the CSV and isolate the specified target column from the input features.
> 3. **Column Filtering:** Drop any feature columns where the percentage of missing values (nulls) OR zero values strictly exceeds the provided threshold parameter (e.g., 95%).
> 4. **Z-Score Normalization:** Apply z-score standardization (mean of 0, standard deviation of 1) to all remaining feature columns across the entire dataset. Do not normalize the target column.
> 5. **Deterministic Splitting:** Calculate an integer `M` representing the number of training samples, based on the provided test set percentage. Split the dataset using a strict index cut: the first `M` samples become the training set, and the remaining samples become the test set. Do not shuffle the data.
> 6. **Export:** Save the resulting datasets (features + target) to `data/training.csv` and `data/test.csv` respectively.
> 
> 
> Ensure the code is clean, well-commented in English, and includes a clear `if __name__ == "__main__":` block to demonstrate how the pipeline is executed using `trial_dataset_ISW.csv` as the input.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: false
    short_description: null
    referenced_in_response: false
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"    # "yes"/"no"  The subsequent part only if "yes"
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 4
  correctness_1_to_5: 4
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "yes"
  notes: null
```

---

## Interaction G27-07-22-003

### 1. LLM and chat information

```yaml
llm_name: "ChatGPT"
llm_version_or_model: "GPT-5"
chat_name_or_identifier: "Python Data Preprocessing Module"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: "trial_dataset_ISW.csv"
      repository_path: "data/trial_dataset_ISW.csv"
      description: "Trial dataset used as context for the preprocessing pipeline."
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: "G27-07-24-002"
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Act as an expert Python software engineer. I am working on a machine learning project with strict structural and algorithmic requirements, and I need you to write the complete code for the data preprocessing module.
The code should be saved as preprocessing.py and placed within my existing repository structure: src/qubo_project/preprocessing.py.
Project Context & Data:
The input dataset is a CSV file containing numeric features and one binary target column (coded as 0 and 1).
The code must be highly efficient as it will eventually be tested on a dataset with over 1,500,000 samples.
For development, we are using a trial dataset named trial_dataset_ISW.csv located in the data/ directory.
The resulting output files must be saved back to the data/ directory as training.csv and test.csv.
Preprocessing Requirements:
Please write a Python script (using libraries like pandas and numpy) that performs the following sequential steps:
Dynamic Inputs: The main execution or function must accept the dataset file path, the exact name of the target column (do not hardcode this), a threshold percentage for dropping columns, and a test set percentage to determine the split.
Target Separation: Read the CSV and isolate the specified target column from the input features.
Column Filtering: Drop any feature columns where the percentage of missing values (nulls) OR zero values strictly exceeds the provided threshold parameter (e.g., 95%).
Z-Score Normalization: Apply z-score standardization (mean of 0, standard deviation of 1) to all remaining feature columns across the entire dataset. Do not normalize the target column.
Deterministic Splitting: Calculate an integer M representing the number of training samples, based on the provided test set percentage. Split the dataset using a strict index cut: the first M samples become the training set, and the remaining samples become the test set. Do not shuffle the data.
Export: Save the resulting datasets (features + target) to data/training.csv and data/test.csv respectively.
Ensure the code is clean, well-commented in English, and includes a clear if __name__ == "__main__": block to demonstrate how the pipeline is executed using trial_dataset_ISW.csv as the input.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
# File: src/qubo_project/preprocessing.py

"""
Preprocessing module for the QUBO Project.

Pipeline:
1. Load a CSV dataset.
2. Separate the target column.
3. Remove feature columns whose percentage of:
- missing values OR
- zero values
strictly exceeds the specified threshold.
4. Apply Z-score normalization to all remaining features.
5. Deterministically split the dataset (no shuffling).
6. Save the resulting training and test datasets.

The implementation is designed to be efficient for very large datasets
(>1.5 million samples) by relying on vectorized pandas/numpy operations.
"""

from pathlib import Path
from typing import Tuple

import numpy as np
import pandas as pd


def preprocess_dataset(
dataset_path: str | Path,
target_column: str,
drop_threshold_percent: float,
test_size_percent: float,
output_dir: str | Path = "data",
) -> Tuple[pd.DataFrame, pd.DataFrame]:
"""
Preprocess a dataset according to the project specifications.

Parameters
----------
dataset_path : str or Path
Path to the input CSV dataset.

target_column : str
Name of the binary target column.

drop_threshold_percent : float
Threshold percentage used to remove feature columns.
A feature is removed if:
%missing > threshold
OR
%zeros > threshold

test_size_percent : float
Percentage of samples assigned to the test set.

output_dir : str or Path, default="data"
Directory where training.csv and test.csv will be saved.

Returns
-------
training_df : pandas.DataFrame
Training dataset (features + target).

test_df : pandas.DataFrame
Test dataset (features + target).
"""

dataset_path = Path(dataset_path)
output_dir = Path(output_dir)
output_dir.mkdir(parents=True, exist_ok=True)

# ------------------------------------------------------------------
# Load dataset
# ------------------------------------------------------------------
df = pd.read_csv(dataset_path)

if target_column not in df.columns:
raise ValueError(f"Target column '{target_column}' not found.")

# ------------------------------------------------------------------
# Separate features and target
# ------------------------------------------------------------------
target = df[target_column]
features = df.drop(columns=[target_column])

n_samples = len(features)

# ------------------------------------------------------------------
# Remove columns with too many missing values
# ------------------------------------------------------------------
missing_percentage = features.isna().mean() * 100.0

# ------------------------------------------------------------------
# Remove columns with too many zero values
# NaNs are not counted as zeros.
# ------------------------------------------------------------------
zero_percentage = features.eq(0).mean() * 100.0

keep_columns = (
(missing_percentage <= drop_threshold_percent)
& (zero_percentage <= drop_threshold_percent)
)

features = features.loc[:, keep_columns]

# ------------------------------------------------------------------
# Z-score normalization
#
# Constant columns (std = 0) become all zeros.
# ------------------------------------------------------------------
means = features.mean()

stds = features.std(ddof=0)

stds = stds.replace(0, 1)

features = (features - means) / stds

# Replace possible NaNs generated during normalization
features = features.fillna(0.0)

# ------------------------------------------------------------------
# Recombine features and target
# ------------------------------------------------------------------
processed = features.copy()
processed[target_column] = target.values

# ------------------------------------------------------------------
# Deterministic split (NO SHUFFLING)
# ------------------------------------------------------------------
test_fraction = test_size_percent / 100.0

M = int(n_samples * (1.0 - test_fraction))

training_df = processed.iloc[:M].reset_index(drop=True)
test_df = processed.iloc[M:].reset_index(drop=True)

# ------------------------------------------------------------------
# Export
# ------------------------------------------------------------------
training_path = output_dir / "training.csv"
test_path = output_dir / "test.csv"

training_df.to_csv(training_path, index=False)
test_df.to_csv(test_path, index=False)

return training_df, test_df


if __name__ == "__main__":
"""
Example execution using the development dataset.

Repository structure:

project_root/
├── data/
│ ├── trial_dataset_ISW.csv
│ ├── training.csv
│ └── test.csv
└── src/
└── qubo_project/
└── preprocessing.py
"""

preprocess_dataset(
dataset_path="data/trial_dataset_ISW.csv",
target_column="target", # Replace with the actual target column name
drop_threshold_percent=95.0,
test_size_percent=20.0,
output_dir="data",
)

print("Preprocessing completed successfully.")
print("Training dataset saved to: data/training.csv")
print("Test dataset saved to: data/test.csv")

Design considerations
This implementation is suitable for large datasets (1.5M+ rows) because it:

Uses vectorized pandas/numpy operations throughout.
Reads the CSV only once.
Computes missing-value and zero-value percentages using efficient column-wise operations.
Performs normalization in a single vectorized pass.
Uses deterministic index slicing (iloc) for the train/test split without shuffling.
Avoids Python loops over rows, which would become a bottleneck on million-row datasets.
The normalization follows your specification by computing the mean and standard deviation over the entire filtered dataset before the deterministic split. Constant columns (standard deviation equal to zero) are safely transformed into all zeros to avoid division-by-zero issues.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G27-07-24-003_preprocessing_code.py"
    repository_path: "reports/llm_usage/files/LLM_G27-07-24-003_preprocessing_code.py"
    file_type: "py"
    created_from_response: true
    short_description: "Generated Python script for the preprocessing pipeline based on the customized prompt."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false  # true / false
  modified_before_use: true   # true / false
  description_of_modifications: "Modified the if __name__ == '__main__': block to use pathlib.Path(__file__).resolve().parents[2] to dynamically resolve the project root. Added debugging print statements (os.getcwd(), path.exists()) in the main function to troubleshoot FileNotFoundError issues."
  related_repository_files:
    - "src/qubo_project/preprocessing.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"
  issue_categories:
    - "runtime_error"
  description: "The LLM's generated code assumed the script would always be executed exactly from the project root directory. Running it from other directories caused a FileNotFoundError because the dataset path was strictly relative ('data/trial_dataset_ISW.csv')."
  how_issue_was_resolved: "Manually updated the script to use dynamic absolute path resolution using __file__ and pathlib."
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 4
  correctness_1_to_5: 3
  clarity_1_to_5: 4
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "no"
  notes: null
```

---

## Interaction  G27-07-DD-002

Copy the structure of `GXX-MM-DD-001` for every new interaction.

---

# Suggested file naming convention

Use unique and descriptive names for every file generated or downloaded from an LLM response.

Recommended format:

```text
LLM_<interaction_id>_<short_description>.<extension>
```

Examples:

```text
LLM_G03-06-05-002_preprocessing_function.py
LLM_G03-06-12-001_qubo_cost_function.py
LLM_G03-06-12-001_test_feature_selection.py
LLM_G03-06-13-003_readme_section.md
```

---

# Final checklist

Before submission, verify that:

- [ ] Every relevant LLM interaction has been recorded.
- [ ] Each interaction has a unique ID.
- [ ] Date and time are present for every interaction.
- [ ] The author is identified as either `couple` or one student matricola.
- [ ] The full prompt is included.
- [ ] The full LLM response is included or correctly referenced as a separate file.
- [ ] Every downloaded/generated file has a unique name, is present in the repository and is correctly referenced in this log.
- [ ] Problems, errors, and hallucinations have been reported honestly.
